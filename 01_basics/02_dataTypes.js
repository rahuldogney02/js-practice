"use strict"; // treat all JS code as newer verson

// alert(3+3); // we are using nodejs, not browser

let name = "Rahul"
let age = 25
let isLoggedIn = false

// number => 2 to 53
//bigint
//string => ""
// boolean => true/false
// null => standandalone value , temprature = null
//undefined => let myName value is not assigned
//symbol => unique


// object
console.table([typeof "etst" , typeof undefined, typeof age, typeof null, typeof isLoggedIn]);

/*
┌─────────┬─────────────┐
│ (index) │ Values      │
├─────────┼─────────────┤
│ 0       │ 'string'    │
│ 1       │ 'undefined' │
│ 2       │ 'number'    │
│ 3       │ 'object'    │
│ 4       │ 'boolean'   │
└─────────┴─────────────┘
*/
